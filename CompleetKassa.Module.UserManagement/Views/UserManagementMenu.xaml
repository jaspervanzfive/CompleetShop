<UserControl x:Class="CompleetKassa.Module.UserManagement.Views.UserManagementMenu"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CompleetKassa.Module.UserManagement.Views"
             mc:Ignorable="d"
			 xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             d:DesignHeight="300" d:DesignWidth="300">
	<Border Background="{Binding Color}" Margin="0,1,0,1">
		<Button Margin="1" BorderBrush="Transparent" Background="{Binding Color}" Command="{Binding OnNavigate}" >
			<StackPanel Orientation="Vertical" VerticalAlignment="Center">
				<Image Source="{Binding ImagePath}" Stretch="UniformToFill" Width="40" Height="40" />
				<Label Content="{Binding Caption}" Padding="0,5,0,0" Foreground="#fff" HorizontalAlignment="Center"/>
			</StackPanel>
		</Button>
	</Border>
</UserControl>
